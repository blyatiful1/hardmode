{
  "//": "Merge these keys into ~/.claude/settings.json — install.sh prints instructions, it does not edit your settings.",
  "effortLevel": "xhigh",
  "env": {
    "CLAUDE_CODE_MAX_OUTPUT_TOKENS": "64000"
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'CONTEXT JUST COMPACTED — recovery protocol: (1) re-read the original request and your task list; (2) the ACTUAL current git state is injected below — trust it over your summary of which files you modified; (3) re-read the current plan step before acting. Do not trust the summary of the summary.'; echo; echo '--- actual git state (post-compaction, deterministic) ---'; git status --short 2>/dev/null | head -30; git diff --stat HEAD 2>/dev/null | tail -8",
            "timeout": 10,
            "statusMessage": "Injecting compaction recovery protocol + git state..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ~/.claude/hooks/stop-claim-audit.py",
            "timeout": 15,
            "statusMessage": "Claim-audit gate..."
          }
        ]
      }
    ]
  }
}
